<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sterlingortiz.smithguru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/roundappicon"
        android:label="@string/app_password_view_tittle"
        android:roundIcon="@mipmap/roundappicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity android:name=".Splashscreen"
            android:label="">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
        </activity>
        
        <activity android:name=".MainActivity">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />

                        <category android:name="android.intent.category.LAUNCH" />
                    </intent-filter>
        </activity>
        <activity android:name=".ViewListContents"
            android:label="@string/app_welcome_main_screen_tittle">
        </activity>
    </application>

</manifest>